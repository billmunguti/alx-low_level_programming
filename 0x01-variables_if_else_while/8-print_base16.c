#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: 0 (Always Success)
 */

int main(void)
{
	char b;

	int d;

	b = 'a';
	while
		(d < 10) {
			putchar(d + '0');
			d++;
		}
	while
		(b <= 'f') {
			putchar(b);
			b++;
		}
	putchar('\n');
	return (0);
}
